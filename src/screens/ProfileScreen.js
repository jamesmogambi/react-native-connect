import * as React from "react";
import { Heading } from "native-base";

// ProfileScreen-
// Allows one to view user profile
const ProfileScreen = () => {
    return (
        <Heading>User Profile</Heading>
    )
}

export default ProfileScreen;

